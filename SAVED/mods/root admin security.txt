not editing root admin in usergroup ACP

controller/admincp/usergroupcontroller

added in edit and delete functions

if($gid == 1) throw new NoPermissionException("You do not have permission to delete the Root Admin group.");


not changing root admin usergroup status in ACP

controller/admincp/usercontroller

added in edit function

	if(!$user->isRootAdmin() && is_numeric($mysidia->input->post("usergroup"))) $mysidia->db->update("users", ["usergroup" => $mysidia->input->post("usergroup")], "uid = '{$user->getID()}'");

model/domainmodel/members

    public function isRootAdmin(){
        if($this->usergroup == 1) return TRUE;
        if($this->usergroup == 2 || $this->usergroup == 0 || $this->usergroup == 3 || $this->usergroup == 4 || $this->usergroup == 5) return FALSE;
    }